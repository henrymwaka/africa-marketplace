# Core framework
Django>=5.0,<5.2
djangorestframework>=3.14

# Database (PostgreSQL)
psycopg2-binary>=2.9

# WSGI server (for production)
gunicorn>=21.2

# Static files & compression
whitenoise>=6.6
Pillow>=10.0   # for image uploads

# CORS handling (API / React frontend)
django-cors-headers>=4.3

# Environment variables
python-dotenv>=1.0

# Optional but useful
requests>=2.31
